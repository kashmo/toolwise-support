const observer=new IntersectionObserver((entries)=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}})},{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const locationCard=document.querySelector('.card-location');if(locationCard)locationCard.style.right='30px';
/* Scroll-linked hero phone: publishes --hero-progress (0 at page top, 1 once the hero has scrolled past). */
(function(){const stage=document.querySelector('.phone-stage'),hero=document.querySelector('.hero');if(!stage||!hero)return;if(window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;const root=document.documentElement;let ticking=false;function update(){ticking=false;const r=hero.getBoundingClientRect();if(!r.height)return;const p=Math.min(1,Math.max(0,-r.top/r.height));root.style.setProperty('--hero-progress',p.toFixed(4))}function request(){if(!ticking){ticking=true;requestAnimationFrame(update)}}update();addEventListener('scroll',request,{passive:true});addEventListener('resize',request,{passive:true})})();

/* Count-up for the stats band. Preserves each stat's prefix/suffix ("< 30 min", "265+"). */
(function(){const wrap=document.querySelector('.numbers');if(!wrap)return;if(window.matchMedia('(prefers-reduced-motion: reduce)').matches)return;const stats=[...wrap.querySelectorAll('strong')].map(el=>{const m=el.textContent.trim().match(/^(\D*?)([\d,]+)(.*)$/);return m?{el:el,pre:m[1],target:parseInt(m[2].replace(/,/g,''),10),suf:m[3]}:null}).filter(Boolean);if(!stats.length)return;const io=new IntersectionObserver(function(entries){entries.forEach(function(e){if(!e.isIntersecting)return;io.unobserve(e.target);const t0=performance.now(),dur=1150;requestAnimationFrame(function step(now){const p=Math.min(1,(now-t0)/dur),k=1-Math.pow(1-p,3);stats.forEach(function(s){s.el.textContent=s.pre+Math.round(s.target*k).toLocaleString()+s.suf});if(p<1)requestAnimationFrame(step)})})},{threshold:.45});io.observe(wrap)})();
